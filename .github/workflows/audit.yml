name: Audit

on:
  workflow_call:
    inputs:
      path:
        required: true
        type: string

jobs:
  scan-result:
    uses: ./.github/workflows/audit_scan.yml
    with:
      path: ${{ inputs.path }}


